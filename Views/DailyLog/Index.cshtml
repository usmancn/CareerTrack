@model List<CareerTrack.Models.Entities.DailyLog>
@{
    ViewData["Title"] = "Staj Defterim";
}

<div class="page-toolbar">
    <span class="text-muted">Toplam @Model.Count kayıt</span>
    <a asp-action="Create" class="btn btn-primary">
        <i class="bi bi-journal-plus me-1"></i> Yeni Günlük
    </a>
</div>

@if (!Model.Any())
{
    <div class="empty-state-full">
        <i class="bi bi-journal-x"></i>
        <h5>Henüz staj günlüğü eklemediniz</h5>
        <a asp-action="Create" class="btn btn-primary mt-2">İlk Günlüğü Ekle</a>
    </div>
}
else
{
    <div class="row g-3">
        @foreach (var log in Model)
        {
            <div class="col-md-6">
                <div class="log-card @(log.IsApprovedByAdmin ? "log-approved" : "")">
                    <div class="log-card-header">
                        <div>
                            <span class="log-day">@log.DayNumber. Gün</span>
                            <span class="log-date">@log.LogDate.ToString("dd MMMM yyyy")</span>
                        </div>
                        <div>
                            @if (log.IsApprovedByAdmin)
                            {
                                <span class="badge bg-success"><i class="bi bi-check-circle me-1"></i>Onaylandı</span>
                            }
                            else
                            {
                                <span class="badge bg-warning text-dark"><i class="bi bi-clock me-1"></i>Onay Bekliyor</span>
                            }
                        </div>
                    </div>
                    <p class="log-content">@(log.Content.Length > 150 ? log.Content.Substring(0, 150) + "..." : log.Content)</p>
                    @if (!string.IsNullOrEmpty(log.AdminNote))
                    {
                        <div class="admin-note">
                            <i class="bi bi-chat-left-text me-1"></i><strong>Danışman:</strong> @log.AdminNote
                        </div>
                    }
                    <div class="log-actions">
                        @if (!log.IsApprovedByAdmin)
                        {
                            <a asp-action="Edit" asp-route-id="@log.Id" class="btn btn-sm btn-outline-warning">
                                <i class="bi bi-pencil"></i> Düzenle
                            </a>
                            <form asp-action="Delete" asp-route-id="@log.Id" method="post" class="d-inline"
                                  onsubmit="return confirm('Bu günlük kaydını silmek istediğinizden emin misiniz?')">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                            </form>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
}
